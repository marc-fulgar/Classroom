<p id="notice"><%= notice %></p>
<% @assignments.each do |assignment| %>
<%= link_to assignment.title, assignment_path(assignment.id) %> <br />
&nbsp;&nbsp;&nbsp;
<%= assignment.content%><br />
<%= assignment.created_at %><br />
<br />
<% end %>

<%= link_to 'Back', lectures_path %>
