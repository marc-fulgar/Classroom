<%= form_with(model: class_subject, local: true) do |form| %>
  <% if class_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_subject.errors.count, "error") %> prohibited this class_subject from being saved:</h2>

      <ul>
      <% class_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
