<%= form_with(model: advisory, local: true) do |form| %>
  <% if advisory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(advisory.errors.count, "error") %> prohibited this advisory from being saved:</h2>

      <ul>
      <% advisory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
